# ============================================
# JWT CONFIGURATION - API ODOO
# ============================================

# JWT Secret Key (CAMBIAR EN PRODUCCIÓN)
# Debe ser una cadena única y compleja de al menos 32 caracteres
# Puedes generar una con: php -r "echo bin2hex(random_bytes(32));"
JWT_SECRET=kJ8$mP2#xL9@qR5&wT3^nV7!bC1*fG4%hD6yN0-CHANGE-ME-IN-PRODUCTION

# Algoritmo de firma JWT
# Opciones: HS256, HS384, HS512, RS256, RS384, RS512
# Recomendado: HS256 para la mayoría de casos
JWT_ALGORITHM=HS256

# Tiempo de expiración del token en horas
# 1 = 1 hora
# 12 = 12 horas
# 24 = 24 horas (recomendado para producción)
# 168 = 7 días
JWT_EXPIRATION_HOURS=24

# ============================================
# CLIENTES AUTORIZADOS - CREDENCIALES
# ============================================

# Cliente de Producción (Odoo Main)
ODOO_CLIENT_001_SECRET=CHANGE-THIS-SECRET-PRODUCTION-2025-XYZ

# Cliente de Desarrollo
ODOO_CLIENT_DEV_SECRET=dev-secret-change-me-ABC123

# Cliente de Testing
ODOO_CLIENT_TEST_SECRET=test-secret-change-me-DEF456

# ============================================
# TOKENS ESTÁTICOS (DEPRECATED - usar JWT)
# ============================================
# Solo usar durante migración, se recomienda JWT
ODOO_VALID_TOKENS=odoo-token-2025,odoo-prod-token

# ============================================
# CONFIGURACIÓN GENERAL ODOO API
# ============================================

# Habilitar logging detallado
ODOO_ENABLE_LOGGING=true

# Timeout de consultas en segundos
ODOO_DB_TIMEOUT=30

# Modo mantenimiento (bloquea el servicio)
ODOO_MAINTENANCE_MODE=false

# ============================================
# INSTRUCCIONES DE USO
# ============================================

# 1. Copiar este archivo a .env
# 2. Cambiar todos los valores que dicen "CHANGE-ME" o "CHANGE-THIS"
# 3. Generar un JWT_SECRET seguro:
#    php -r "echo bin2hex(random_bytes(32));"
# 4. Configurar secretos únicos para cada cliente
# 5. En producción usar HTTPS siempre

# ============================================
# EJEMPLO DE GENERACIÓN DE SECRET SEGURO
# ============================================

# Opción 1: PHP
# php -r "echo bin2hex(random_bytes(32));"

# Opción 2: OpenSSL
# openssl rand -hex 32

# Opción 3: Bash
# tr -dc A-Za-z0-9 </dev/urandom | head -c 64

# ============================================
# RECOMENDACIONES DE SEGURIDAD
# ============================================

# ✅ Usar secretos de al menos 32 caracteres
# ✅ Usar caracteres alfanuméricos y especiales
# ✅ Cambiar secretos cada 6 meses en producción
# ✅ No compartir secretos por email o slack
# ✅ Usar variables de entorno en producción
# ✅ Rotar secretos si hay sospecha de compromiso
# ✅ Usar HTTPS en producción
# ✅ Configurar expiración apropiada según uso

# ============================================
# CONFIGURACIÓN POR AMBIENTE
# ============================================

# DESARROLLO
# JWT_EXPIRATION_HOURS=24
# ODOO_ENABLE_LOGGING=true

# STAGING
# JWT_EXPIRATION_HOURS=12
# ODOO_ENABLE_LOGGING=true

# PRODUCCIÓN
# JWT_EXPIRATION_HOURS=24
# ODOO_ENABLE_LOGGING=false
# ODOO_MAINTENANCE_MODE=false
